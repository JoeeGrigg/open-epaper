import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  label: {
    fontSize: 15,
    marginBottom: 5
  },
  description: {
    marginTop: 5
  },
  input: {
    borderColor: '#000',
    borderWidth: 2,
    fontSize: 20,
    borderRadius: 5,
    padding: 10,
    overflow: 'hidden'
  }
});